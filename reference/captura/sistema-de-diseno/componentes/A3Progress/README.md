# A3Progress

Avance de un A3 como segmentos: pasos hechos sobre el total (7 por defecto).

Copia exacta de `a3-progress.tsx`: segmentos de 5px en `color-tinta` (hechos) y `color-borde` (pendientes), cifra en `display` tabular. En la captura sirve igual para «4/6 fases con criterios de salida».
